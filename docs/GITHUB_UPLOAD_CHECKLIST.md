# GitHub Upload Checklist

Before uploading:

- [ ] Open `README.md` and check that the project description looks right.
- [ ] Confirm that `.pptx`, `.docx`, `.zip`, `.DS_Store`, `__pycache__`, and local cache files are not included.
- [ ] Confirm that `data/champion_icons/` is not included.
- [ ] Confirm that no personal student ID or private path is included in the README.
- [ ] Run `pip install -r requirements.txt` in a clean environment if you want to test execution.
- [ ] Open `notebooks/비정형네트워크프로젝트.ipynb` and verify it renders on GitHub.
- [ ] Upload the folder contents to a new GitHub repository.

Suggested repository description:

```text
LoL pro match OP champion composition, synergy network, counter network, and prediction feature analysis.
```

Suggested topics:

```text
league-of-legends, network-analysis, web-crawling, data-analysis, machine-learning, python, networkx
```

