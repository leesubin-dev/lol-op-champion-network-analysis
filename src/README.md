# Source Code

`pipeline_from_notebook.py` is auto-extracted from the main notebook so reviewers can inspect the project logic in a plain Python file.

The primary runnable artifact is:

```text
notebooks/비정형네트워크프로젝트.ipynb
```

Recommended review order:

1. Read `README.md` for the project summary.
2. Open the notebook for the full analysis flow.
3. Use `pipeline_from_notebook.py` for quick code search or line-by-line review.
4. Check `outputs/` and `figures/` for generated CSV and PNG results.

